<!DOCTYPE html>
<html>
    <head>
        <title>Students</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
        <link rel="stylesheet" href="/stylesheets/main.css">
        <script src="https://code.jquery.com/jquery-git2.min.js"></script>
        <script src="/scripts/setRole.js"></script>        
    </head>
    <body>
    <nav class="navbar navbar-default">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand" href="/login">login</a>
                <a class="navbar-brand role_al role_wa" href="/users">Users</a>                
                <a class="navbar-brand" href="/events">Events</a>
                <a class="navbar-brand role_sc" href="/students">Students</a>
                <a class="navbar-brand role_al role_wa" href="/schools">Schools</a>
            </div>
            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav navbar-right">
                    <% if(!currentUser){ %>
                        <li><a href="/login">Login</a></li>
                        <li><a href="/requestpwreset">Register</a></li>
                    <% } else { %>
                        <li><a href="#">Signed In As <%= currentUser.name %></a></li>
                        <li><a href="/logout">Logout</a></li>
                        <span class="hidden" id="userRole"><%= currentUser.role %></span>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
    
    <div class="container">
       <% if(error && error.length > 0) { %>
         <div class="alert alert-danger"><p><%=error %></p></div>
       <% } %>
       
       <% if(success && success.length > 0) { %>
         <div class="alert alert-success"><p><%= success %></p></div>
       <% } %>
    </div>
    <div class="container">
       
