<% include ../partials/header %>

<%

function dateTimeString(d) {
    var dayNames = ["Sun","Mon","Tue","Wed","Thu","Fri","Sat"];
    var h=d.getHours();
    var ap;
    if (h>12) {
        h=h-12;
        ap="PM";
    }
    else {
        ap="AM";
    }
    var m = d.getMinutes();
    if (m==0) {
        m="00";
    }
    return dayNames[d.getDay()] + " " + (d.getMonth() + 1) + "/" + d.getDate() + "/" + (d.getFullYear() - 2000) + " " + h + ":" + m + " " + ap;
}

%>
<% include ../../public/passport.html %>
<% include ../../public/parentInfo.html %>
<% include ../../public/parentInfoSp.html %>
<script>
$(document).ready(function() {
    $("#year").text(new Date().getFullYear());
    $("#hdgName").text('<%= student.fname + " " + student.lname %>');
    $("#name").text('<%= student.fname + " " + student.lname %>');
    $("#grade").text('<%= student.grade %>');
    $("#schoolName").text('<%= student.school.name %>');
    $("#user").text('<%= currentUser.name %>');
    $("#sched").text('<%= dateTimeString(student.slot.sdate) %>');    
});

$("#parentShopCB").on('change', function (e) {
    // console.log("parent shop CB changed to " + this.checked);
    if (this.checked) {
        $("#parentShopText").removeClass("hidden");
        $("#parentShopText").removeClass("hidden-print");
    }
    else {
        $("#parentShopText").addClass("hidden");
        $("#parentShopText").addClass("hidden-print");
    }
});

$("#spanParInfoCB").on('change', function (e) {
    // console.log("Span par info CB changed to " + this.checked);
    if (this.checked) {
        $("#engParInfo").addClass("hidden");
        $("#engParInfo").addClass("hidden-print");
        $("#spanParInfo").removeClass("hidden");
        $("#spanParInfo").removeClass("hidden-print");
    }
    else {
        $("#engParInfo").removeClass("hidden");
        $("#engParInfo").removeClass("hidden-print");
        $("#spanParInfo").addClass("hidden");
        $("#spanParInfo").addClass("hidden-print");
    }
});

function myFunction() {
    window.print();
}
</script>

<% include ../partials/footer %>

